# Auto-generated from apps.yaml - DO NOT EDIT MANUALLY
# Regenerate with: ./scripts/generate-argocd-state.sh local
#
# NOTE: You must build and push the image before ArgoCD can deploy this:
#   docker build -t k3s-platform/serverless-example:latest apps/serverless-example
#   docker push k3s-platform/serverless-example:latest
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: serverless-example
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: k3s-platform
    environment: local
    type: serverless
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: k3s-platform
  source:
    repoURL: https://github.com/GeeekyBoy/k3s-platform.git
    targetRevision: HEAD
    path: argocd-state/local/serverless/serverless-example
    directory:
      recurse: false
      include: 'manifests.yaml'
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
