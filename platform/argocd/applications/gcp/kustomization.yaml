apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GCP environment ArgoCD Applications
# These applications manage all workloads in the GCP k3s cluster

resources:
  - valkey.yaml
  - fastapi.yaml

labels:
  - pairs:
      environment: gcp
      cloud: gcp
      app.kubernetes.io/part-of: k3s-platform
    includeSelectors: false
