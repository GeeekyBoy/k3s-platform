# K3s Platform Configuration - LOCAL (On-Premises Production)
# Copy this file to configs/.env for on-premises production-like deployment
#
# This environment is for:
#   - On-premises deployment without cloud provider
#   - Production-like settings (HA, autoscaling)
#   - No hot-reload (use dev environment for that)
#   - Full KEDA scale-to-zero support
#   - ArgoCD for GitOps deployments

#===============================================================================
# Environment Type
#===============================================================================
PLATFORM_ENV="local"

#===============================================================================
# Cluster Configuration
#===============================================================================
CLUSTER_NAME="k3s-local"

#===============================================================================
# Local Registry (k3d built-in)
#===============================================================================
REGISTRY_NAME="registry.localhost:5111"

#===============================================================================
# Application Configuration
#===============================================================================
# Valkey password (auto-generated if empty)
VALKEY_PASSWORD=""

# KEDA cooldown period (seconds before scaling to zero)
KEDA_COOLDOWN_PERIOD="300"

#===============================================================================
# Scaling Configuration
#===============================================================================
# Minimum workers for local cluster (set to 0 for scale-to-zero)
MIN_WORKERS="1"
MAX_WORKERS="3"
